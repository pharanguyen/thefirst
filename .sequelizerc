const path = require('path');
module.exports = {
    'config': path.resolve('./src/config', 'config.json'),
    'migrations-path': path.resolve('./src', 'migrations'),// cach tạo các bảng trong database
    'models-path': path.resolve('./src', 'models'),//tìm đến thư mục models
    'seeders-path': path.resolve('./src', 'seeders')//tạo dữ liệu fetch
}